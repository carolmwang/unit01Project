// 900px board, 3px wide, 30 cells

let imageBoard = document.querySelector('.game-board');
for (let i = 0; i < 90; i += 1) {
    let gameCell = document.createElement('div');
    gameCell.className = 'cell';
    imageBoard.appendChild(gameCell);
    }


let 



